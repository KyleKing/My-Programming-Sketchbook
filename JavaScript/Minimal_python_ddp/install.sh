# Install browser-refresh package globally
npm install -g browser-refresh;

# Install all of the local node modules
npm install;

# Run index.js (Uses the "main" property of package.json)
browser-refresh;
